"""qlib integration layer for k-atana.

qlib (pyqlib) requires Python 3.8–3.12. The k-atana backend venv currently
runs Python 3.13, so pyqlib cannot be installed directly. This package is
written to run on a Python <=3.12 interpreter with pyqlib installed.

See docs/spikes/xar-417-qlib-spike.md for the full verdict and migration path.
"""
